{
  "name": "esolang-battle-2-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w apps/backend",
    "dev:frontend": "npm run dev -w apps/frontend",
    "build": "npm run build --workspaces",
    "generate": "npm run generate -w apps/backend"
  },
  "type": "module",
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
